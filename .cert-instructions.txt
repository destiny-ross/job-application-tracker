17

// once
mkcert -install to create a local CA

// project directory
mkcert localhost 127.0.0.1 ::1 
to create a trusted cert for localhost in the current directory
You're using node (which doesn't use the system root store), so you need to specify the CA explicitly in an environment variable, e.g: export 

// .env
NODE_EXTRA_CA_CERTS="$(mkcert -CAROOT)/rootCA.pem"


// server.js

const https = require('https');
const fs = require('fs');
const express = require('express');

const app = express();    
const server = https.createServer({
    key: fs.readFileSync('localhost+2-key.pem'), 
    cert: fs.readFileSync('localhost+2.pem'), 
    requestCert: false,
    rejectUnauthorized: false,
}, app).listen(10443); // get creative
