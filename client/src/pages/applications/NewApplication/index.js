import Page from "../../../components/Page";

const NewApplication = () => {
  return (
    <Page title="New Application" displayHeader={true} displaySubheader={false}>
      Form.
    </Page>
  );
};

export default NewApplication;
