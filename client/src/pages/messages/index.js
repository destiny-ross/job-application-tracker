import Page from "../../components/Page";

const MessagesPage = () => {
  return (
    <Page title="Messages" displayHeader={true} displaySubheader={false}>
      Messages
    </Page>
  );
};

export default MessagesPage;
