import Page from "../../components/Page";

const SettingsPage = () => {
  return (
    <Page title="Settings" displayHeader={true} displaySubheader={false}>
      Settings
    </Page>
  );
};

export default SettingsPage;
